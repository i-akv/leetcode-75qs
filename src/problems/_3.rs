pub struct Solution{}
impl Solution {
    pub fn kids_with_candies(candies: Vec<i32>, extra_candies: i32) -> Vec<bool> {
        let mut initial_candies: u32 = 0;
        for c in candies.iter() {
println!("{}", c);
        }
        vec![true, false]
    }
}